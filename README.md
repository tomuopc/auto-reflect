# AI自动写reflect

一个用于辅助填写 `reflect.today` 的浏览器插件。

## 说明

- 支持按照预设内容作为系统提示词和用户提示词，自动生成、填写、发布日报。
- 支持只填写部分选项。

## 首次使用
- 安装插件
- 点击插件，进入提示词设置页面
- 设置 系统提示词 和 用户提示词 并**保存**。

## API密钥
在`config.js`中设置API密钥等信息。
